<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <parameters>
        <parameter key="axstrad.browser_sync.event_listener.class">Axstrad\BrowserSyncBundle\EventListener\BrowserSyncListener</parameter>
        <parameter key="axstrad.browser_sync.mode">2</parameter>
        <parameter key="axstrad.browser_sync.server_ip">2</parameter>
    </parameters>

    <services>
        <service id="axstrad.browser_sync.event_listener" class="%axstrad.browser_sync.event_listener.class%">
            <tag name="kernel.event_subscriber" />
            <argument type="service" id="twig" />
            <argument>%axstrad.browser_sync.server_ip%</argument>
            <argument>%axstrad.browser_sync.mode%</argument>
        </service>
    </services>
</container>
